<h1>文章列表</h1>

<%= link_to "新增文章", new_post_path %>

<table>
  <thead>
    <tr>
      <th>標題</th>
      <th>內文</th>
    </tr>
  </thead>

  <tbody>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.content %></td>
    </tr>
  <% end %>
  </tbody>
</table>